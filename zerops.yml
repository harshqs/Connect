zerops:
  # ── Backend: Express REST API + Yjs WebSocket server ──────────────────────
  - setup: api
    build:
      base: nodejs@22
      buildCommands:
        - npm ci --include=dev
        - npx prisma generate
        - npx prisma migrate deploy
      deployFiles:
        - server/
        - prisma/
        - node_modules/
        - package.json
      cache:
        - node_modules
        - package-lock.json
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: 3000
      start: node_modules/.bin/tsx server/index.ts
      healthCheck:
        httpGet:
          port: 3000
          path: /health

  # ── Frontend: Next.js (standalone output) ─────────────────────────────────
  - setup: frontend
    build:
      base: nodejs@22
      buildCommands:
        - npm ci --include=dev
        - npx prisma generate
        - npm run build
      deployFiles:
        - .next/standalone/
        - .next/static/  => .next/standalone/.next/static/
        - public/        => .next/standalone/public/
      cache:
        - node_modules
        - package-lock.json
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: 3000
        NODE_ENV: production
        HOSTNAME: 0.0.0.0
      start: node .next/standalone/server.js
      healthCheck:
        httpGet:
          port: 3000
          path: /
