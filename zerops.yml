zerops:
  # ── Backend: Express REST API + Yjs WebSocket server ──────────────────────
  - setup: api
    build:
      base: nodejs@22
      buildCommands:
        - npm ci --include=dev
        - npx prisma generate
      deployFiles:
        - server/
        - prisma/
        - node_modules/
        - package.json
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: 3000
      initCommands:
        # Run any pending DB migrations before the process starts.
        # Safe to run on every deploy — already-applied migrations are skipped.
        - npx prisma migrate deploy
      start: node_modules/.bin/tsx server/index.ts
      healthCheck:
        httpGet:
          port: 3000
          path: /health

  # ── Frontend: Next.js (standalone output) ─────────────────────────────────
  - setup: frontend
    build:
      base: nodejs@22
      buildCommands:
        - npm ci --include=dev
        - npx prisma generate
        - npm run build
      deployFiles:
        # standalone bundle — includes server + required node_modules
        - .next/standalone/
        # static assets must be copied in alongside standalone
        - .next/static/  => .next/standalone/.next/static/
        - public/        => .next/standalone/public/
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        PORT: 3000
        NODE_ENV: production
        HOSTNAME: 0.0.0.0
      start: node .next/standalone/server.js
      healthCheck:
        httpGet:
          port: 3000
          path: /
